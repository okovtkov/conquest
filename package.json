{
    "name": "conquest",
    "private": true,
    "scripts": {
        "build": "weboops --mode build --publicPath /conquest/ -o ./docs/",
        "dev": "weboops --mode development -o ./docs/"
    },
    "dependencies": {
        "@weboops/core": "^0.1.6"
    }
}